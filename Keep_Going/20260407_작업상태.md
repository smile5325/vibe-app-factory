# Vibe App Factory — 작업 상태 로그

- 날짜: 2026-04-07
- 버전: v1.2.4
- 작업자: Claude Code

---

## 작업 내용

### SKILL 2개 buildHarnessPrompt() 적용

**적용 SKILL:**
1. `script_length_master.md` → STEP 3 대본 분량 제어
2. `category_design_matcher.md` → STEP 4 카테고리 비주얼 스타일 매핑

**수정 파일:** `src/App.jsx`

**변경 위치 3곳:**

#### 1. 함수 상단 변수 추가 (charBlock 아래)
```javascript
const scriptTarget = { short/mid/long30/long60 }[length]
const scriptSection = { short/mid/long30/long60 }[length]
const visualStyle = { finance/tech/business/.../other }[category]
```

#### 2. STEP 3 대본 앞 삽입
```
[대본 분량 필수 준수]
목표 글자수: ${scriptTarget}
기승전결 배분: ${scriptSection}
전개 순서: 훅 → 공감 → 팩트 → 스토리 → 인사이트 → 반전 → CTA
```

#### 3. STEP 4 charBlock 위 삽입
```
[VISUAL STYLE — 전 씬 공통 적용]
COLOR & TONE: ${visualStyle}
```

---

## Git 커밋
- 메시지: `feat: 대본 분량 타겟 + 카테고리 비주얼 스타일 v1.2.4`
- 해시: f534b02
- 브랜치: main → push 완료

---

## 다음 할 일
- [ ] 실제 생성 테스트 — 미드폼 선택 시 대본 2300자 이상 확인
- [ ] travel 카테고리 선택 시 sky blue + sunset orange 프롬프트 반영 확인
- [ ] Vercel 배포 반영 확인

---

## 2세션 추가 작업 (v1.2.5)

### STEP 4 대사 기반 이미지 연동 Sync

**목적:** STEP 4 비주얼 생성 시 STEP 3 대본의 실제 대사 기반으로 이미지 프롬프트 강제 연동

**수정 파일:** `src/App.jsx`

**수정 내용 (3곳):**

1. `runPipeline()` STEP 4 실행 직전 — scriptOutput + scriptRef 변수 추가
   ```js
   const scriptOutput = allOutput.find(o => o.step.id === 3)?.content || "";
   const scriptRef = scriptOutput
     ? `\n\n[STEP 3 대본 참조 — 반드시 아래 대사 기준으로 이미지 프롬프트 생성]\n${scriptOutput.slice(0, 3000)}`
     : "";
   ```

2. `userPrompt` 끝에 조건부 삽입:
   ```js
   ...${step.id === 4 ? scriptRef : ""}
   ```

3. `buildHarnessPrompt()` STEP 4 섹션 맨 앞에 [대사-이미지 연동 규칙] 블록 삽입

**Git 커밋:**
- 메시지: `feat: STEP 4 대사 기반 이미지 연동 Sync v1.2.5`
- 해시: 6753b13
- 브랜치: main → push 완료

**다음 할 일 (v1.2.5 이후):**
- [ ] STEP 4 실행 후 대사 기반 이미지 프롬프트 생성 확인 (실 API 테스트)
- [ ] AI 주제 생성 버튼 작동 확인
- [ ] XLSX 비주얼 시트 9열 + 행 간격 육안 확인
- [ ] 비주얼 탭 뱃지 GENERAL/CORE 색상 확인
